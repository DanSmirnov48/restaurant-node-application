<html>
    <head>
        {{>header}}
    </head>
    <body>
        <div class="container mx-auto mt-4">
            {{^chefs_special}}
                <div class="d-flex justify-content-center">
                    <div class="btn-group" role="group" aria-label="...">
                        <a href="/dishes/all" class="btn btn-dark">All</a>
                        <a href="/dishes/lunch" class="btn btn-dark">Lunch</a>
                        <a href="/dishes/dinner" class="btn btn-dark">Dinner</a>
                    </div>
                </div>
            {{/chefs_special}}

            <div class="row mt-4">
                {{#entries}}
                    <div class="col-md-4">
                    <div class="card">
                        <img src='../{{dishImage}}' class="card-img-top" alt="../{{dishImage}}" />
                        <div class="card-body">
                            <h5 class="card-title">{{dishName}}</h5>
                            <h6 class="card-subtitle mb-2 text-muted">£{{dishPrice}}</h6>
                            <p class="card-text">{{dishDesc}}</p>

                            {{#chefs_special}}
                                <a href="/dishes/{{dishName}}" class="btn btn-dark">View</a>
                            {{/chefs_special}}

                            {{^chefs_special}}
                                {{^user}}
                                    <a href="/dishes/{{dishName}}" class="btn btn-dark">View</a>
                                {{/user}}
                                {{#user}}
                                    <a href="/dishes/{{dishName}}" class="btn btn-dark">View</a>
                                    <a href="/update/{{dishName}}" class="btn btn-dark">Update</a>
                                    <a href="/delete/{{dishName}}" class="btn btn-dark">Delete</a>
                                {{/user}}
                            {{/chefs_special}}
                        </div>
                    </div>
                    </div>
                {{/entries}}
                {{^entries}}
                    <div class="center-container">
                        <h1 class="text-center">Nothing here just yet</h1>
                    </div>
                {{/entries}}

            </div>
        </div>

        <style>
            .card {
                width: 20rem;
                background: #222;
                border: 1px solid #dd2476;
                color: rgba(250, 250, 250, 0.8);
                margin-bottom: 2rem;
            }
            body {
                background: #111;
            }
            .center-container {
                display: flex;
                align-items: center;
                justify-content: center;
                height: 100%;
            }
            h1{
                color: white
            }
        </style>

    </body>
</html>